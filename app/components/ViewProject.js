'use client'
import React from 'react'

const ViewProject = () => {
  return (
    <div>
      <button className='btn btn-neutral' onClick={() => console.log('click')}>View Projects</button>
    </div>
  )
}

export default ViewProject